services:
  app:
    build: .
    image: dj-organizer
    ports:
      - "3001:3001"
    volumes:
      - ./server/uploads:/app/server/uploads
      - ./server/processed:/app/server/processed
    environment:
      - PORT=3001
    restart: unless-stopped
