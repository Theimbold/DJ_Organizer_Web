# DJ Organizer Web

## Setup
Run `npm install` to install dependencies.

## Development
Start the development server with `npm run dev`.

## Build
Build the project for production with `npm run build`.

## Rights & Permissions
Ensure you have the correct permissions for accessing files.

## Export Modes
Details on how to export files will be provided later.
